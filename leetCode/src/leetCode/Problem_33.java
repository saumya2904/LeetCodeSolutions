package leetCode;

public class Problem_33 {
    public int search(int[] nums, int target) {
    	int low = 0;
    	int high = low + (nums.length-low)/2;
    	int mid = (low+high)/2;
    	if(mid == target) {
    		return mid;
    	}
    	while(low <= high) {
    		
    	}
		return target;
        
    }
}
